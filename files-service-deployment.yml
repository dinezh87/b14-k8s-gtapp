apiVersion: apps/v1
kind: Deployment
metadata:
  name: files-service-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      component: files-service
  template:
    metadata:
      labels:
        component: files-service
    spec:
      serviceAccountName: files-service-sa
      containers:
        - name: files-service
          image: prashantdey/merndemoapp:fileservice1.0
          ports:
            - containerPort: 5004
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          env:
            - name: PORT
              value: "5004"
            - name: AWS_REGION
              value: "us-west-1"
            - name: S3_BUCKET
              value: "gratitude-b14"
            - name: S3_PREFIX
              value: "gratitude-uploads"
            - name: FILE_MAX_MB
              value: "10"
